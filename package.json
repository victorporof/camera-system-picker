{
  "name": "lenses",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "babel-node index.js",
    "check:cameras": "babel-node src/check-cameras.js",
    "check:lenses": "babel-node src/check-lenses.js",
    "build:camera-links": "mkdir -p build; babel-node src/build-camera-links.js",
    "build:lens-links": "mkdir -p build; babel-node src/build-lens-links.js",
    "build:camera-data": "mkdir -p build; babel-node src/build-camera-data.js",
    "build:lens-data": "mkdir -p build; babel-node src/build-lens-data.js",
    "mounts": "babel-node src/make-mounts.js"
  },
  "dependencies": {
    "delay": "^4.3.0",
    "lodash": "^4.17.15",
    "moment": "^2.29.2",
    "puppeteer": "^5.5.0"
  },
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "@babel/node": "^7.5.5",
    "@babel/preset-env": "^7.5.5"
  }
}
